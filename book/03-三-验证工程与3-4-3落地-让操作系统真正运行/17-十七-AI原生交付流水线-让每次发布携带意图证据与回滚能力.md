# 第十七章｜AI 原生交付流水线：让每次发布携带意图、证据与回滚能力


前面的章节已经回答了如何签署意图、建立约束、编排 Agent、生成测试、汇聚 Evidence Bundle 和作出发布裁决。但企业真正落地时还会遇到一组无法绕开的工程问题：门禁在哪里执行？多个 Agent 如何隔离修改？测试通过的提交与最终发布的制品是不是同一个？契约、代码、证据和版本如何对应？Agent 是否可以直接合并主干或发布生产？

这些问题属于 CI/CD、DevOps、分支和版本管理的领域。本书不重复介绍 Git 命令、Jenkins 安装或 Kubernetes 操作，而关注它们在 AI Coding 时代承担的新治理职责。

核心判断是：

> **CI/CD 负责把代码安全地送到生产；3-4-3 负责证明这份代码为什么有资格被送到生产。**

## 17.1 3-4-3 不取代 DevOps，而是补上意图与证据

DevOps 解决开发、交付、运行与反馈之间的协作和自动化；CI 持续集成代码并执行构建与验证；CD 让合格制品持续交付或部署；Harness 把权限、约束、门禁、观测和恢复变成 Agent 无法随意绕过的运行环境；SCOPE-V 则控制一项意图如何从规格走向证据和裁决。

它们处在不同层次，不能互相替代：

```text
Intent Contract：为什么做、做到什么程度
Constraint Matrix：什么边界不可突破
Work Graph：任务、测试与集成怎样编排
CI Pipeline：构建、测试、检查并采集证据
Evidence Bundle：为什么可以作出当前裁决
CD Pipeline：制品怎样晋级、灰度、部署和回退
Telemetry：上线后是否产生真实价值和异常
```

没有 DevOps 底座，3-4-3 的可执行门禁很难规模化；只有 DevOps 而没有 3-4-3，流水线可以高效发布代码，却未必知道代码对应哪项业务意图、风险由谁接受、证据是否足以支持上线。

因此，Agentic Agile 不是在现有流水线外再搭建一条平行流程，而是把契约、约束、证据和遥测嵌入企业已有的 Git、CI/CD、制品库、发布平台和可观测系统。

## 17.2 为什么 AI 流水线必须前移到 Commit 之前

传统流水线通常从代码提交开始：

```text
Commit → Build → Test → Scan → Deploy
```

但 AI Coding 的许多风险发生在 Commit 之前：Agent 是否读取了正确版本的契约，是否获得了过大的数据和工具权限，是否修改了未授权文件，是否擅自增加依赖，测试是否在实现前产生，多个 Agent 是否正在覆盖彼此的工作，以及 Token、时间与重试是否已经超过预算。

因此，AI 原生交付流水线要向前延伸：

```text
意图签署
→ 上下文、权限与基线校验
→ 分支或 Worktree 隔离
→ TDD Red
→ Agent 实现与增量约束检查
→ 多层验证与集成
→ Evidence Bundle 与独立 Verify
→ 人类发布裁决
→ 不可变制品晋级与部署
→ 生产遥测和 Bug 回溯
```

这里的“流水线”不再只是 CI 服务器中的几个 Job，而是从 Agent 获得任务到生产反馈回流的完整受控通道。Commit 只是其中一个事件，不是治理起点。

## 17.3 分支管理首先解决隔离，而不是命名格式

传统团队讨论分支策略时，常在 GitFlow、Trunk-Based Development 和 Feature Branch 之间选择。AI Coding 时代仍需这些策略，但首先要解决一个更基础的问题：高速并行的 Agent 是否拥有清晰、隔离、可追溯的写入空间。

如果多个 Agent 直接共享一个工作目录或分支，可能出现：

- 一个 Agent 覆盖另一个 Agent 尚未提交的修改；
- 测试基于不同代码基线运行，结果却被混在一起；
- 某 Agent 顺手提交了其他节点的未完成文件；
- 合并冲突被 AI 语法正确地解决，却改变了业务语义；
- Evidence Bundle 无法说明某项证据究竟对应哪一组代码；
- 代码验证完成后又有新提交进入，最终发布内容发生漂移。

所以，每个可独立执行的 Work Graph 节点至少要绑定：

- Task ID、Contract ID 和节点 ID；
- 起始 Commit 或制品基线；
- 分支、Worktree 或受控沙箱；
- 文件所有权与允许修改范围；
- 上游依赖版本；
- 允许使用的工具和凭证；
- 合并目标和汇合节点；
- 提交、合并和废弃条件；
- 对应证据与遥测位置。

分支名称是否使用 `agent/T-101-api` 并不是重点。重点是系统能否从分支追溯契约，从节点看见允许范围，从合并结果反查参与的 Agent 和证据。

## 17.4 默认建议：受保护主干、短分支或 Worktree、Merge Queue

对于多数 AI Coding 团队，一个合理默认是：

> **受保护主干＋短生命周期任务分支或 Worktree＋自动门禁＋Merge Queue。**

```text
main（受保护）
  ├─ agent/T-101-test
  ├─ agent/T-101-api
  └─ agent/T-101-ui
           ↓
    按 Work Graph 汇合
           ↓
   集成验证与证据聚合
           ↓
      Verify / HITL
           ↓
       Merge Queue
           ↓
          main
```

短生命周期分支减少长期漂移，Worktree 允许同一仓库为多个 Agent 提供物理隔离的工作目录，受保护主干阻止未验证结果直接进入稳定基线，Merge Queue 则在最新主干上重新验证候选合并，解决“每个分支单独都绿，组合以后失败”的问题。

这不是唯一正确策略。高监管行业、需要长期维护多个客户版本或存在固定发布列车的产品，仍可能需要 Release Branch；极小、低风险、单 Agent 项目可以直接在主干进行受控的小提交；紧急修复也可能使用 Hotfix Branch。书中不把某种流派绝对化，判断标准是：

- 是否降低并行冲突和基线漂移；
- 是否保持提交、证据和制品可追溯；
- 是否让风险匹配适当门禁；
- 是否支持快速集成而不是制造长期分叉；
- 是否能够安全废弃失败的 Agent 结果。

## 17.5 文件所有权与语义冲突

Git 能发现同一行文本冲突，却无法发现语义冲突。一个 Agent 修改金额规则，另一个 Agent 修改审批路由，两边可能没有任何文本冲突，合并后却形成互相矛盾的行为。Graph Engineering 因此需要把文件所有权进一步提升为能力和契约所有权。

对于高耦合节点，可以采用：

- 预先声明写集合，Graph Engine 检测重叠；
- 同一文件只允许一个节点写，其他节点通过 Handoff 提建议；
- 接口由跨模块契约管理，提供方和消费方分别验证；
- 汇合后重新运行组合级 AC、SIT 和 NFR；
- 对关键业务冲突设置模块 OA 或全局 OA 审查；
- 不允许 Agent 仅因合并无文本冲突就宣布集成成功。

以报销案例为例，测试 Agent 可以独立写边界测试，领域 Agent 修改金额判断，接口 Agent 适配响应格式，UI Agent 修改提示与审批展示。但它们汇合后必须重新证明：5000.00 元进入普通审批、5000.01 元进入总监审批、接口和页面表达一致、历史草稿没有被迁移。

## 17.6 从代码版本升级为交付谱系

传统版本管理主要关注 Commit、Tag、软件版本号和发布制品。AI Coding 时代还要管理影响结果的认知与治理资产：

- Intent Graph 的版本；
- Intent Contract 的版本和签署状态；
- Constraint Matrix 与 Tools Manifest 的版本；
- Work Graph、Context Slice 和 Handoff 版本；
- Agent、模型、Prompt 和 Skill 版本；
- 测试、Golden Set、数据集和 Schema 版本；
- Evidence Bundle 与发布裁决版本；
- Artifact Digest、配置和部署环境版本；
- 契约级遥测及生产事件。

这些版本共同形成 **Delivery Lineage（交付谱系）**：

```text
Intent Contract T-101 v3（已签署）
        ↓
Constraint Matrix commit A
        ↓
Context Slice hash B
        ↓
Code commit C
        ↓
Test / SIT / NFR results for C
        ↓
Evidence Bundle EB-T-101
        ↓
Artifact digest sha256:D
        ↓
Release 2.4.1
        ↓
Production telemetry for T-101
```

交付谱系回答的不是“当前版本号是多少”，而是：生产环境中的这份制品，依据哪份意图，由哪些 Agent 在什么约束下生成，经过哪些真实验证，由谁接受风险，并在上线后表现如何。

如果测试报告对应 Commit C，Evidence Bundle 却没有记录版本；如果发布平台从最新主干重新构建了 Commit D；或者契约签署后约束矩阵已经发生重大变化，证据链就已经断裂。

## 17.7 建立 Release Manifest

Evidence Bundle 面向人类解释“为什么可以裁决”，还可以增加一份机器可读的 Release Manifest，固定发布对象的谱系关系：

```yaml
release_id: R-2.4.1
task_id: T-101
contract:
  path: governance/contracts/Intent_Contract_T-101.md
  version: 3
  signed_at: 2026-08-07T10:30:00+08:00
governance:
  constraints_commit: a1b2c3d
  work_graph_hash: sha256:...
source:
  repository: expense-service
  commit: c4d5e6f
artifact:
  image: registry.example.com/expense:2.4.1
  digest: sha256:...
evidence:
  bundle: governance/evidence/EB-T-101.md
  verdict: CONDITIONALLY_APPROVED
  approved_by: Finance-IO
deployment:
  strategy: canary
  initial_traffic: 10%
  rollback_to: sha256:previous...
```

这份清单不一定采用相同字段，但至少应绑定契约、源码、治理基线、证据裁决、不可变制品、部署策略和回退对象。CI 可以自动生成大部分字段，人类只负责不能自动推导的风险接受和发布条件。

## 17.8 CI 如何承载五道机械门

五道门不是要求所有检查都放在同一个 CI Job，而是说明不同时间点必须有什么事实。它们可以映射到本地 Harness、Pull Request、Merge Queue、发布流水线和事后回溯：

| 门 | 典型执行位置 | CI/CD 要证明什么 |
|---|---|---|
| Pre 前置门 | Agent 启动前、本地或任务编排器 | 契约真实签署、约束与 AC 已准备 |
| Coding 编码门 | 分支首次实现前 | 测试先写并产生可信 Red |
| Prove 验证门 | PR 与候选合并流水线 | 测试、AC、构建、NFR 与证据通过 |
| Closing 收尾门 | 发布候选生成前 | Evidence Bundle、遥测、Dashboard 和图谱回写完整 |
| Bug 回溯门 | 生产缺陷处理流水线 | 原契约、证据、首次成功率和知识已如实修正 |

Pre 与 Coding 发生在 Commit 之前，因此不能只依赖远程 CI；它们可以由本地 Hook、Agent Harness 或任务平台机械执行。Prove 适合在 PR 和 Merge Queue 中运行。Closing 连接验证结果、发布裁决与制品。Bug 门则由事故或缺陷事件触发。

门禁应满足三条工程要求：

1. **不可静默绕过**：跳过、允许失败或管理员强制合并必须形成例外记录；
2. **结果可消费**：使用结构化输出、稳定退出码和原始日志，而不是只显示一个绿色图标；
3. **结论可追溯**：每项结果绑定 Task ID、Commit、环境和时间，并进入 Evidence Bundle。

## 17.9 Build Once, Verify Once, Promote Many

传统 DevOps 强调 Build Once, Deploy Many：代码构建一次，产生不可变制品，同一制品逐级部署，避免不同环境重复构建出不同结果。在 3-4-3 中还要再强调一步：被验证和裁决的必须是最终晋级的那一份制品。

```text
Source Commit C
      ↓ 一次构建
Artifact Digest D
      ↓
测试 / 扫描 / SIT / 性能
      ↓
Evidence Bundle 绑定 D
      ↓
Verify 与发布裁决
      ↓
D 晋级 UAT / 预生产 / 生产
```

不能出现“Commit C 通过测试，Agent 随后又提交 D，发布平台从最新代码重新构建”的情况；也不能在 UAT 环境测试镜像 A，却在生产重新构建镜像 B。版本号或镜像标签可能被覆盖，Artifact Digest 才是更可靠的不可变身份。

因此应遵循：

- CI 只从明确 Commit 构建；
- 制品进入受控 Registry，禁止覆盖同一 Digest；
- Evidence Bundle 和 Release Manifest 记录 Digest；
- 环境晋级使用同一制品，只改变经过治理的环境配置；
- 部署后再次校验运行中的 Digest；
- 若源码、依赖或制品变化，原裁决自动失效并重新验证。

这条规则可以概括为：**验证过的就是发布的，发布的就是被裁决的。**

## 17.10 版本号、Tag 和配置也要受治理

语义化版本可以表达兼容性意图，但版本号本身不是证据。Agent 可以建议 Major、Minor 或 Patch，OA 和模块负责人仍需确认变更性质。破坏性接口变更不能因为 Agent 自动把版本号加一就获得许可，还需要消费方影响分析、跨模块契约更新和迁移策略。

Git Tag 应指向已经形成 Release Manifest 的 Commit；发布说明应从真实契约、代码差异和已知风险生成，而不是由模型凭聊天记忆总结。配置、Feature Flag、数据库 Schema 和基础设施同样属于版本化交付物，因为同一代码在不同配置下可能产生完全不同的行为。

对于模型型或 Agent 型产品，还要记录模型、Prompt、Skill、工具和 Golden Set 的版本。生产行为变化不一定来自代码，如果这些非代码资产没有版本，事故回溯将找不到真实原因。

## 17.11 CD 中的人机权限边界

AI 可以在交付流水线中承担大量工作：创建任务分支和 Worktree，提交代码，创建 Pull Request，运行 CI，分析失败，在预算内自动修复，生成发布说明，部署测试环境，准备灰度计划，观察指标并汇总证据。

但自动化能力不能等同于授权。以下行为通常需要明确批准：

- 合并到受保护主干；
- 使用或扩大生产凭证；
- 修改生产配置和安全策略；
- 执行破坏性数据库迁移；
- 跳过或降低 MUST 门禁；
- 扩大灰度流量或全量发布；
- 删除生产数据或执行不可逆补偿；
- 接受高风险例外；
- 撤回或覆盖已有发布裁决。

一个实用原则是：

```text
AI 可以准备发布
AI 可以证明发布条件
AI 可以执行已授权、可恢复的步骤
人类负责价值判断、风险接受和不可逆裁决
```

并非每次部署都要让高管点击按钮。团队可以通过风险分级进行预授权：低风险、可逆、证据完整的变更由 CD 自动晋级；资金、隐私、权限和不可逆数据变更触发 HITL；任何 MUST 失败都不能进入自动发布。

## 17.12 灰度、Feature Flag 与条件批准

发布不是只有“上线”和“不上线”两个选项。Evidence Bundle 可能支持条件批准：允许指定用户、地区、部门、时间或流量范围内发布，并绑定观察指标和撤回条件。

Feature Flag、金丝雀、蓝绿和影子流量是实现这种治理决定的工程手段：

- Feature Flag 将代码部署与功能启用分离；
- 金丝雀逐步扩大真实流量；
- 蓝绿发布保留可快速切换的旧环境；
- 影子流量在不影响用户的情况下比较新旧行为。

这些手段并不会自动降低风险。Feature Flag 如果没有负责人和失效日期，会变成长期配置债务；金丝雀如果没有明确成功指标，只是把事故延迟发生；蓝绿如果数据库迁移不可逆，也无法真正回切。

条件批准至少要写明：范围、持续时间、观察指标、阈值、负责人、扩大条件、停止条件和回退目标。CD 把这些条件转成自动步骤，Dashboard 显示实时状态，人类不必在每个正常步骤中介入，只在边界被触发时裁决。

## 17.13 回滚不只是重新部署旧版本

应用制品回滚通常容易，数据、消息、外部副作用和跨模块状态却可能已经向前推进。一个可靠回退计划至少要分析：

- 下游节点是否已经消费新事件；
- 数据库迁移是否可逆或需要补偿；
- 新旧版本能否同时读取当前 Schema；
- 缓存、队列和搜索索引是否需要恢复；
- Feature Flag 关闭后是否停止新行为；
- 已发生的资金、通知和外部调用如何处理；
- 回退是否影响同一并行组的其他任务。

因此，CD 的回滚动作应受 Work Graph 和 `verify_rollback_safety` 一类检查约束。Agent 可以分析依赖、生成命令和在沙箱演练，但高风险生产回退仍需要授权。Evidence Bundle 中的“可回滚”应来自真实演练或可执行验证，不应只是一句计划。

## 17.14 从发布版本回到契约级遥测

发布完成后，生产观测必须能够从 Artifact Digest 或 Release ID 回到 Contract ID。否则错误率上升时，团队只能看到“2.4.1 有问题”，却不知道它改变了哪项业务意图、由谁裁决、当时接受了什么风险。

理想链路是：

```text
生产事件 / 指标
→ Release ID 与 Artifact Digest
→ Source Commit
→ Contract ID 与 Evidence Bundle
→ 对应 AC、约束和发布条件
→ Bug 回溯、遥测修正和图谱学习
```

单契约 Dashboard 可以显示该任务发布后的错误率、性能、业务命中和异常审批比例；全局 Dashboard 则观察不同版本、团队、模型或分支策略对首次成功率、HITL、Token、部署失败和恢复时间的影响。

这里仍需避免传统 DevOps 指标崇拜。部署频率更高不一定代表价值更高，变更前置时间更短也不代表风险更低。DORA 类工程指标可以与 3-4-3 的目标准确率、首次成功率、证据完整度、Token 效率和知识沉淀率联动阅读，而不是单独追求某个数字。

## 17.15 一个端到端的携证发布案例

以 T-101 报销金额规则为例：

1. IO 签署 Intent Contract v3，OA 固定约束和当前主干 Commit；
2. Graph 为测试、领域、接口和 UI 节点创建独立 Worktree，声明写集合；
3. 测试节点先产生可信 Red，Coding 门保留结果；
4. 各 Agent 在授权范围内实现，Harness 监视文件、依赖和工具调用；
5. 分支分别通过单元和接口检查，再按 Work Graph 汇合；
6. Merge Queue 在最新主干候选上运行组合 AC、SIT、安全和性能测试；
7. CI 从候选 Commit 只构建一次制品，记录 Artifact Digest；
8. Evidence Bundle 将每条 AC、MUST、测试结果和 Digest 建立映射；
9. 财务负责人完成 UAT Acceptance，IO 条件批准两个部门灰度 24 小时；
10. CD 将同一 Digest 部署到灰度环境，Feature Flag 限定范围；
11. Dashboard 观察异常审批率、错误率、p95 和用户反馈；
12. 指标正常后按已批准策略扩大流量；若超过阈值，自动关闭开关并升级人工；
13. 生产版本、遥测和教训回写 T-101，而不是成为一条无归属的运维记录。

这条链说明，分支、CI/CD 和版本管理并不是 3-4-3 之外的技术细节，而是把意图、约束和证据送达生产的物理通道。

## 17.16 不同规模团队的最小配置

治理不应要求所有团队一开始建设同样复杂的平台。

| 场景 | 分支与隔离 | CI/CD 最小要求 | 版本与证据 |
|---|---|---|---|
| 单人、低风险 | 短分支或单 Worktree | 测试、Harness、Closing 门 | Commit＋契约＋Evidence Bundle |
| 小团队、多 Agent | 受保护主干＋任务 Worktree | PR 门禁＋Merge Queue＋不可变制品 | Release Manifest＋Digest |
| 多模块系统 | 模块分支＋集成 DAG | 跨模块契约、SIT、证据聚合 | 模块证据＋发布级证据包 |
| 强监管、高风险 | 职责分离＋Release Branch | 独立环境、签名制品、多级批准 | 完整谱系、审计、SBOM、长期留存 |

不论规模多小，仍要守住三个底线：最终代码必须对应签署意图，验证结果必须对应唯一版本，发布制品必须与被裁决制品一致。

## 17.17 常见失败

- Agent 直接在共享主干写入，多个任务的修改和证据混在一起；
- 分支存在很久，契约和主干已变化却没有重新裁剪上下文；
- PR 单独全绿，合并后不在最新基线重新验证；
- AI 自动解决语义冲突，没有重新运行组合 AC；
- 测试报告没有 Commit、环境和时间；
- Evidence Bundle 记录源码版本，却没有记录 Artifact Digest；
- UAT 测试的是制品 A，生产重新构建并部署制品 B；
- 使用可覆盖的 `latest` 标签作为发布身份；
- Agent 可以关闭分支保护或将门禁设为允许失败；
- Release Note 由模型生成，却与真实契约和变更不一致；
- Feature Flag 没有负责人、到期时间和清理计划；
- 把“重新部署旧镜像”误认为完整数据回滚；
- 发布指标无法关联 Contract ID，生产缺陷成为无归属事件；
- 为追求部署频率，把必要证据和 HITL 视为效率障碍。

## 17.18 本专题的工程验收

团队可以选择一项真实变更，验证以下问题：

1. 能否从任务分支反查签署契约、起始基线和允许修改范围？
2. 多个 Agent 的写入是否隔离，汇合顺序是否进入 Work Graph？
3. CI 的测试和门禁是否绑定唯一 Commit，并保存原始结果？
4. Evidence Bundle 是否绑定最终 Artifact Digest？
5. UAT、Verify 和发布裁决面对的是不是同一份不可变制品？
6. Agent 是否无法自行绕过受保护主干、MUST 门禁和生产权限？
7. 灰度、回退、负责人和触发阈值是否可执行并经过验证？
8. 生产指标能否回到 Release ID、Contract ID 和 Evidence Bundle？

如果这些问题只能靠某位工程师回忆，组织拥有的是自动化工具；如果都能由系统和制品回答，组织才拥有一条可验证的 AI 原生交付流水线。


## 17.19 本章小结

AI 原生交付流水线把契约、分支、Commit、测试、Evidence Bundle、Artifact Digest、发布裁决和生产遥测连接成完整交付谱系。CI/CD 负责把合格制品安全送向生产，3-4-3 负责证明该制品为什么有资格被交付。受保护主干、隔离工作区、机械门、Build Once、条件批准、灰度和可验证回退，共同确保 Agent 不能绕过责任与证据直接发布。

**本章最小实践**：把一个真实契约接入现有 CI/CD，生成绑定 Commit 与 Artifact Digest 的发布证据。  
**完成证据**：Release Manifest、Evidence Bundle、门禁记录、发布裁决、灰度与回退验证。  
**延伸实践**：推荐学习《Agentic Agile 3-4-3 基础课：跑通第一次受控自治》，把意图、证据和回滚能力带进 AI 原生交付流水线。

---

## B. 3-4-3 工程化落地与扩展

前面的 A 组说明 3-4-3 怎样在一次任务中运行。B 组继续回答三个工程问题：新项目和既有大型系统从哪里开始，知识与记忆怎样成为可信基础设施，多人、多工具、多模块怎样从局部闭环扩展为可组合交付。

这三章仍属于“验证工程与 3-4-3 落地”，重点是工程承载和规模化接口，不提前讨论岗位体系、组织激励或完整转型路线。人员、组织、流程和工具怎样共同成为企业运行方式，将在第四部分集中展开。
